<div class="bw-page">
  <div class="bw-header">
    <h1>Movimientos</h1>
    <p class="bw-sub">Últimos movimientos de tus cuentas</p>
  </div>
  <div class="bw-card">
    <% if (!movimientos.length) { %>
      <div class="bw-empty">Sin movimientos recientes.</div>
    <% } else { %>
      <table class="bw-table">
        <thead>
          <tr>
            <th>Fecha</th>
            <th>Concepto</th>
            <th style="text-align:right">Importe</th>
            <th>Estado</th>
          </tr>
        </thead>
        <tbody>
          <% movimientos.forEach(function(m) { %>
            <tr>
              <td class="bw-muted" style="white-space:nowrap"><%= new Date(m.createdAt).toLocaleString('es-ES') %></td>
              <td><%- m.concept || m.kind %></td>
              <td class="<%= m.esEntrada ? 'bw-pos' : 'bw-neg' %>" style="text-align:right">
                <%= (m.esEntrada ? '+' : '−') %><%= m.amountPz.toLocaleString('es-ES') %> Pz
              </td>
              <td><span class="bw-chip <%= m.status === 'Settled' ? 'bw-chip-soft' : 'bw-chip-warn' %>"><%= m.status %></span></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </div>
</div>
